{
	"name": "json-to-typescript",
	"version": "0.0.3",
	"description": "Converts a JSON object to TypeScript interfaces",
	"keywords": ["json", "typescript", "typings", "dts"],
	"author": "Alexander Abashkin <a.abashkin@corp.mail.ru>",
	"license": "MIT",

	"repository": {
		"type": "git",
		"url": "git@github.com:monolithed/json-to-typescript.git"
	},

	"engines": {
		"node": ">=4.8",
		"npm": ">=3"
	},

	"scripts": {
		"test": "node tests",
		"prepublishOnly": "tsc"
	},

	"dependencies": {
		"generate-schema": "^2.3.3",
		"json-schema-to-typescript": "^3.1.4",
		"deep-filter": "^1.0.1"
	},

	"devDependencies": {
		"@types/debug": "^0.0.29",
		"@types/node": "^7.x",
		"typescript": "^2.2.1"
	}
}
